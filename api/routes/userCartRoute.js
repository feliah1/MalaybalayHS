const express = require("express")
const router = express.Router()
const { getAllProdCart } = require("../controllers/userCartController")

// router.route("/get-all-in-cart").get(getAllProdCart)

module.exports = router;